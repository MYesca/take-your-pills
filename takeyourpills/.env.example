# Note: 
# - NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Variables without NEXT_PUBLIC_ are server-side only
# - Never commit .env.local to version control
# - Copy this file to .env.local and fill in your actual values

# Database Configuration
# PostgreSQL connection string for the external database server
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://user:password@localhost:5432/takeyourpills"

# Environment
# Set to "development" for local development, "production" for production
NODE_ENV="development"

# Microsoft Entra External ID Configuration
# These values are obtained from your Azure portal after setting up Microsoft Entra External ID
# Client ID (Application ID) from Azure App Registration
NEXT_PUBLIC_AZURE_CLIENT_ID="your-azure-client-id-here"
# Tenant ID (Directory ID) from Azure App Registration
NEXT_PUBLIC_AZURE_TENANT_ID="your-azure-tenant-id-here"
# Redirect URI - must match the redirect URI configured in Azure App Registration
# For local development, typically: http://localhost:3000
# For production, use your production domain: https://yourdomain.com
NEXT_PUBLIC_AZURE_REDIRECT_URI="http://localhost:3000"

# Client Secret for backend token validation (server-side only, not exposed to client)
# This is obtained from Azure App Registration > Certificates & secrets
AZURE_CLIENT_ID="your-azure-client-id-here"
AZURE_CLIENT_SECRET="your-azure-client-secret-here"
AZURE_TENANT_ID="your-azure-tenant-id-here"